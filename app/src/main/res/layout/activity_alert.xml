<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorPrimary">

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/push_animation_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:lottie_rawRes="@raw/push"
            app:lottie_loop="true"
            app:lottie_autoPlay="true"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginTop="88dp"/>

        <TextView
            android:id="@+id/comment_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/push_animation_view"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginTop="33.7dp"
            android:textSize="20sp"
            android:textColor="@color/colorText"
            android:letterSpacing="-0.05"
            android:lineSpacingExtra="12sp"
            android:gravity="center_horizontal"/>

        <com.google.android.material.button.MaterialButton
                android:id="@+id/finish_alert"
                android:layout_width="0dp"
                android:layout_height="52dp"
                android:backgroundTint="@color/colorAccent"
                android:text="@string/finish_alert_text"
                android:textColor="#ffffff"
                android:textSize="16sp"
                android:letterSpacing="-0.04"
                android:lineSpacingExtra="20sp"
                android:padding="0dp"
                app:cornerRadius="5dp"
                android:elevation="0dp"
                app:layout_constraintTop_toBottomOf="@id/comment_view"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                android:layout_marginLeft="16dp"
                android:layout_marginRight="16dp"
                android:layout_marginTop="71.3dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>